# Archive Documentation

Ce dossier contient les documents historiques deplaces pour garder `docs/` propre.

## Dossiers
- `generalization/` : rapports fuzz et mesures de generalisation.
- `audits/` : exports visuels et audits map/assets.
- `legacy/` : anciens plans et specs remplaces.
- `map/` : ancien dossier map conserve pour compatibilite historique.

## Regle
Ces fichiers sont conserves pour reference historique, mais ne sont plus la source de verite.
